C# RAT (Remote Access Tool) - server which can access information about client computers through screenshots and can restrict certain applications or the usage of workstations (the application goes into full screen and blocks the mouse movement until the server sends an unlock message). The application also has commands for shutdown or restart client workstation.
Technologies: C#, TCP

For educational purpose, have fun !